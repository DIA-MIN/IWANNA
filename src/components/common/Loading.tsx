import React from 'react';
import './common.scss';

const Loading = () => {
  return (
    <div className="loading__box">
      <div className="loader"></div>
    </div>
  );
};

export default Loading;
